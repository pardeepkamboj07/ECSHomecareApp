import { EquateDatePipePipe } from './equate-date-pipe.pipe';

describe('EquateDatePipePipe', () => {
  it('create an instance', () => {
    const pipe = new EquateDatePipePipe();
    expect(pipe).toBeTruthy();
  });
});
